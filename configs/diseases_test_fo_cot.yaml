# Benchmark Configuration - Diseases Test (40 diseases, FO + COT)
# Run: python benchmark_runner.py --config configs/diseases_test_fo_cot.yaml

models:
  seeker:
    model: "Qwen3-8B"
    base_url: "http://10.100.0.121:8020/v1"
    timeout: 120.0
    extra_body:
      chat_template_kwargs:
        enable_thinking: true

  oracle:
    model: "Qwen3-8B"
    base_url: "http://10.100.0.121:8020/v1"
    timeout: 120.0

  pruner:
    model: "Qwen3-8B"
    base_url: "http://10.100.0.121:8020/v1"
    timeout: 120.0

game:
  observability_mode: "FULLY_OBSERVABLE"
  max_turns: 25

dataset:
  type: "diseases"
  csv_path: "data/diseases/diseases_test.csv"
  num_targets: null
  runs_per_target: 1

experiment:
  name: "diseases_test_fo_cot"
  max_workers: 12

output:
  base_dir: "outputs"
  save_conversations: true
  save_graph_plots: false

debug:
  enabled: true
